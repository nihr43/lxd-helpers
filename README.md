# lxd-helpers

```
usage: main.py [-h] [--rebalance REBALANCE] [--drain DRAIN]

options:
  -h, --help            show this help message and exit
  --rebalance REBALANCE
                        Given a string prefix, rebalance matching instances
                        accross cluster nodes
  --drain DRAIN         Drain a cluster node
```
